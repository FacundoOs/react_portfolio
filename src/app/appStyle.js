import { makeStyles } from '@material-ui/core/styles'
import useStyle from '../views/About/aboutStyle'

const useStyles = makeStyles({
  particlesCanva: {
    position: 'absolute',
    top: 0,
    left: 0,
  },
})

export default useStyles
